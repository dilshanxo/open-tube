# Contributing to OpenTube

1. Follow the AI Engineering Constitution in `.agents/agents.md`.
2. Ensure strict adherence to the layered architecture.
3. Run `pytest`, `ruff check .`, and `mypy .` before submitting PRs.
4. Follow Conventional Commits.
